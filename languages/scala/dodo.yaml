backdrops:
  scala:
    image:
      name: dodo/languages/scala
      requires: [dodo/base]
      context: '{{ currentDir }}'
      dockerfile: '{{ currentDir }}/Dockerfile'
    interactive: true

  sbt:
    image:
      name: dodo/languages/scala
      requires: [dodo/base]
      context: '{{ currentDir }}'
      dockerfile: '{{ currentDir }}/Dockerfile'
    volumes:
      - '{{ projectRoot }}:/work'
      - 'sbt-cache:/root'
    working_dir: /work
    script: exec sbt "$@"
