backdrops:
  git:
    image:
      requires: dodo/base
      context: '{{ currentDir }}'
      dockerfile: '{{ currentDir }}/Dockerfile'
    volumes:
      - 'sshconfig:/root/.ssh'
      - '{{ projectRoot }}:/project'
    working_dir: '/project/{{ projectPath }}'
    script: exec git "$@"
