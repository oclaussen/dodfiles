backdrops:
  git:
    image:
      name: dodo/git
      requires: [dodo/base, dodo/languages/python]
      context: '{{ currentDir }}'
      dockerfile: '{{ currentDir }}/Dockerfile'
    volumes:
      - 'gpg-data:/data/gpg'
      - 'sshconfig:/home/dodo/.ssh'
      - '{{ projectRoot }}:/project'
    environment:
      - 'SSH_AUTH_SOCK=/data/gpg/S.gpg-agent.ssh'
    working_dir: '/project/{{ projectPath }}'
    script: exec lab "$@"
