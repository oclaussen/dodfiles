backdrops:
  vim:
    aliases: vi
    image:
      name: dodo/vim
      requires: [dodo/base, dodo/languages/golang]
      context: '{{ currentDir }}'
      dockerfile: '{{ currentDir }}/Dockerfile'
    volumes:
      - '{{ cwd }}:/work'
    working_dir: /work
    script: exec nvim "$@"
